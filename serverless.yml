service: mba-backend

frameworkVersion: '3'

useDotenv: true

plugins:
  - serverless-esbuild

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1
  stage: dev
  environment:
    OPENAI_API_KEY: ${env:OPENAI_API_KEY}
    LARAVEL_API_URL: ${env:LARAVEL_API_URL}
    LARAVEL_TOKEN: ${env:LARAVEL_TOKEN}

functions:
  agent:
    handler: src/agent/index.handler
    events:
      - httpApi:
          path: /pergunta
          method: POST

custom:
  esbuild:
    bundle: true
    format: esm
    target: node20
    platform: node
    external:
      - "@langchain/openai"
      - "@langchain/core"

package:
  include:
    - src/**
    - package.json
    - package-lock.json



